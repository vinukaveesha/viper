services:
  jenkins:
    # Enable inline mode: the code-review CLI runs directly inside Jenkins
    # rather than spawning a separate agent container, which avoids the need
    # for a working Docker socket inside the Codespace.
    environment:
      USE_INLINE_AGENT: "true"
