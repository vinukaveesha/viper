# code-review-agent dependencies
# Pinned to latest stable versions at implementation time
# Regenerate with: pip freeze > requirements.lock

google-adk>=1.25.0
httpx>=0.28.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
typer>=0.12.0
litellm>=1.50.0

# Dev
pytest>=8.3.0
pytest-asyncio>=0.24.0
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
pygments>=2.20.0 # not directly required, pinned by Snyk to avoid a vulnerability
