// Single-SCM entry point: uses global environment variables (SCM_PROVIDER, SCM_URL).
// Set them in Manage Jenkins → System → Global properties → Environment variables.
// For multiple SCMs, use Jenkinsfile.multi-scm-wrapper per job instead; see docs/JENKINS-MULTIPLE-SCMS.md.
//
// Credentials: ID "SCM_TOKEN", ID "GOOGLE_API_KEY" (folder or global).

load 'docker/jenkins/mainPipeline.groovy'
